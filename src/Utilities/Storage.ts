const STORAGE_KEYS = {
  AUTH0_TOKEN: '@mcatprep__auth0Token',
  THEME_STORAGE_KEY: '@mcatprep__theme',
  IS_EXPLORED: '@mcatprep__isExplored',
  IS_ONBOARDED: '@mcatprep__isOnBoarded',
  IS_SCHEDULED: '@mcatprep__isScheduled',
};

export default STORAGE_KEYS;
